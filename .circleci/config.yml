defaults: &defaults
  docker:
    - image: typelead/eta:latest

version: 2.0
jobs:
  tests:
    <<: *defaults
    steps:
      - checkout

      - restore_cache:
          key: v1201-etlas-cache-

      - run: etlas update
      - run: etlas install --dependencies-only
      - run: etlas run

      - save_cache:
          key: v1201-etlas-cache-{{ epoch }}
          paths:
            - "~/.etlas/config"
            - "~/.etlas/packages"
            - "~/.etlas/patches"
            - "~/.etlas/tools"

workflows:
  version: 2
  full_cycle:
    jobs:
      - tests
